/**
 * Simple representation for repeated object name.
 */
export class Name {
  /**
   * @param firstName The first name of the object.
   * @param lastName The last name of the object.
   */
  constructor(public firstName, public lastName) {}
}
